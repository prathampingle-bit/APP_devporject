name: room_manager_app
description: Flutter frontend-only scaffold for Room Management project
publish_to: 'none'
version: 0.1.0+1
environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  go_router: ^7.0.0
  flutter_riverpod: ^2.3.6
  dio: ^5.0.0
  flutter_secure_storage: ^8.0.0
  intl: ^0.20.2
  uuid: ^3.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
